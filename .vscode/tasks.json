{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup",
            "type": "shell",
            "command": "meson setup build"
        },
        {
            "label": "build",
            "type": "shell",
            "command": "meson install -C build --destdir .\\install"
        },
        {
            "label": "launch",
            "type": "shell",
            "command": "./launch.bat"
        }
    ]
}